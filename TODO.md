# 大学课程安排系统 TODO（手动编码版）

## 0. 约束对齐（先确认）
- [x] `R0-1` 采用 `C++ + TUI` 作为第一阶段界面形态（替代 docx 中“图形界面”）。
- [x] `R0-2` 保留 docx 的两个编排策略：
  - 均匀负担（各学期学分尽量均衡）
  - 前置集中（尽量把课程排到前几个学期）
- [x] `R0-3` 课程字段按“并集”实现：课程号、课程名、学分、先修课程列表。
- [x] `R0-4` 硬约束：学期总数上限（如 8）、单学期学分上限、每门课占 1 学期、先修关系必须满足。
- [x] `R0-5` 无解时明确报错（例如：先修有环、超出学期上限、学分上限导致不可排）。

## 1. 第一阶段：TUI 课程安排系统

## 1.1 工程骨架
- [x] `P1-1` 建立目录：`src/`, `include/`, `data/`, `output/`, `docs/`。
- [x] `P1-2` 在 `src/main.cpp` 建立菜单循环（先留空功能入口）。
- [x] `P1-3` 定义核心结构体：
  - `Course { id, name, credit, prereq_ids }`
  - `PlanConfig { max_semesters, max_credits_per_semester, strategy }`
  - `SemesterPlan { vector<course_id>, total_credits }`

完成标准：
- 程序可启动并显示菜单；结构体可编译通过。

## 1.2 文本导入与导出
- [x] `P1-4` 设计课程目录 txt 格式（建议）：
  - 第一行：课程数 `N`
  - 后续每行：`课程号|课程名|学分|先修1,先修2,...`
  - 无先修时最后一列留空
- [x] `P1-5` 实现 `importCoursesFromTxt(path)`，包含输入合法性校验：
  - 课程号唯一、格式合法（先按 3 位字母数字）
  - 学分为正数
  - 先修课程号必须存在
- [x] `P1-6` 实现 `exportPlanToTxt(path)`，按学期输出课程和学分总计。

完成标准：
- 可从当前目录导入课程文件并生成内存课程表；
- 可导出排课结果到 `output/` 下 txt。

## 1.3 课程目录编辑（docx 基本要求）
- [x] `P1-7` TUI 菜单支持：新增课程、删除课程、修改课程、查看课程列表。
- [x] `P1-8` 删除课程时检查被依赖关系（提示并阻止或要求级联处理）。

完成标准：
- 不重启程序即可编辑课程目录并保持数据一致性。

## 1.4 先修图与可行性判断
- [x] `P1-9` 建图（有向图：先修 -> 后继）。
- [x] `P1-10` 实现拓扑排序（Kahn 或 DFS 均可）。
- [x] `P1-11` 检测无解场景并给出具体原因：
  - 有环
  - 单门课程学分超过单学期上限
  - 在学期上限内无法安排完

完成标准：
- 任意输入都能给出“可排课”或“不可排课 + 原因”。

## 1.5 两种编排策略
- [x] `P1-12` 策略 A：`BALANCED`（负担尽量均匀）。
- [x] `P1-13` 策略 B：`FRONT_LOADED`（尽量前置）。
- [x] `P1-14` 在菜单中让用户选择策略并生成课表。

完成标准：
- 同一课程集在两种策略下输出结果有明显差异且都满足约束。

## 1.6 测试样例（手工）
- [x] `P1-15` 准备 4 组测试数据：
  - 正常可排（无先修）
  - 正常可排（多层先修）
  - 不可排（有环）
  - 不可排（学分/学期上限冲突）
- [x] `P1-16` 每组记录“输入、预期、实际输出”到 `docs/test-report.md`。

完成标准：
- 至少 4 组样例可复现，报告可提交。

## 2. 第二阶段：数据库连接（Navicat 配套）

说明：`Navicat Premium 16` 是数据库管理客户端，建议后端数据库使用 `MySQL 8.x`（或老师指定数据库）。

## 2.1 数据库表设计
- [ ] `P2-1` 建 3 张表：
  - `student(student_id PK, name, major, grade)`
  - `course(course_id PK, course_name, credit)`
  - `student_course(id PK, student_id FK, course_id FK, semester, score, status)`
- [ ] `P2-2` 在 Navicat 中创建并导出 SQL 脚本到 `docs/schema.sql`。

完成标准：
- 三表可创建、外键关系正确、可插入示例数据。

## 2.2 C++ 连接数据库
- [ ] `P2-3` 选连接方案（推荐其一）：
  - MySQL Connector/C++
  - ODBC
- [ ] `P2-4` 实现最小连接与查询：列出某学生已修课程。
- [ ] `P2-5` 把第一阶段导出的课表写入 `student_course`（至少包含学号、课程号、学期、状态）。

完成标准：
- 命令行能完成“读学生历史 -> 写新排课结果”闭环。

## 2.3 业务联动
- [ ] `P2-6` 排课前读取学生已修课程，自动从待排课程中剔除。
- [ ] `P2-7` 防止重复插入（同学生同课程同学期）。

完成标准：
- 同一学生重复执行排课不会出现脏数据。

## 3. 代码提交节奏（建议）
- [ ] `G-1` 每完成一个任务号提交一次（例如 `feat: finish P1-10 topo sort`）。
- [ ] `G-2` 每天结束前更新本文件打勾状态与“卡点”。

## 4. 你和 Agent 的协作方式
- [ ] 你每次只说：`继续 TODO: P1-10`（或多个编号）。
- [ ] 我会按该编号给你：
  - 实现思路（你手写）
  - 函数签名建议
  - 常见错误检查清单
  - 你写完后我帮你 review

## 5. 当前建议起步顺序
- [x] `NEXT-1` 先做 `P1-1 ~ P1-3`
- [x] `NEXT-2` 再做 `P1-4 ~ P1-6`
- [x] `NEXT-3` 然后做 `P1-9 ~ P1-11`
- [x] `NEXT-4` 最后补 `P1-12 ~ P1-16`
