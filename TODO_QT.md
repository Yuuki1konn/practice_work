# Qt 可视化改造 TODO（根目录工作清单）

## 0. 目标与约束
- [ ] `QT-0-1` 保留现有 C++ 业务层（`scheduler/course_io/course_edit/db_odbc`），Qt 只做界面层。
- [ ] `QT-0-2` 数据库账号密码只输入一次（登录后复用会话）。
- [ ] `QT-0-3` 所有写库操作统一二次确认（`是否上传到数据库？`）。
- [ ] `QT-0-4` 保持“可读、易改”：UI/Service/DB 分层，避免窗口里直接写 SQL。

## 1. 工程基础
- [x] `QT-1-1` 建立 `qt_ui/` 子目录，创建最小 Qt 程序入口。
- [x] `QT-1-2` 配置 `CMakeLists.txt`，可编译 `scheduler_qt.exe`。
- [x] `QT-1-3` 完成登录窗口（数据库连接成功后进入主窗口）。
- [x] `QT-1-4` 主窗口骨架（按钮入口占位）。
说明：
- 主界面当前保留 3 个入口：`学生管理 / 课程管理 / 计划管理`
- `导出课表` 已并入 `计划管理` 页面，不再单独保留主界面按钮

完成标准：
- 可启动 Qt 程序并完成一次数据库登录。

## 2. 会话与通用组件
- [ ] `QT-2-1` 完善 `AppSession`（保存连接状态、当前登录用户、错误信息）。
- [x] `QT-2-2` 增加统一消息组件（成功/失败/确认弹窗封装）。
- [x] `QT-2-3` 增加统一“写库确认”函数。

完成标准：
- 任意页面可复用同一会话与确认逻辑。

## 3. 学生管理页（优先）
- [x] `QT-3-1` 新建 Student 页面，表格展示 `student_id/name/major/grade`。
- [x] `QT-3-2` 新增学生（表单 + 校验 + 刷新表格）。
- [x] `QT-3-3` 修改学生（行选择 + 回填 + 保存）。
- [x] `QT-3-4` 删除学生（二次确认）。
- [x] `QT-3-5` 查看某学生课程历史（联动 `student_course` + `course`）。

完成标准：
- 在 UI 中可完整完成学生增删改查与课程历史查看。

## 4. 课程管理页
- [x] `QT-4-1` 课程表格展示缓存课程（含先修列表）。
- [x] `QT-4-2` 支持导入来源二选一（TXT / 数据库）。
- [x] `QT-4-3` 支持缓存区新增/修改/删除课程。
- [x] `QT-4-4` 同步课程到数据库（`course` + `course_prereq`，带确认弹窗）。

完成标准：
- 课程缓存与数据库可双向同步，先修关系不丢失。

## 5. 排课页
- [x] `QT-5-1` 配置排课参数（学期上限、学分上限、策略）。
- [x] `QT-5-2` 选择学生后显示学生信息与历史课程。
- [x] `QT-5-3` 支持“剔除已修课程”并生成排课计划。
- [x] `QT-5-4` 结果表格展示每学期课程和总学分。

完成标准：
- 在 UI 中完成“选学生 -> 剔除已修 -> 生成课表”。

## 6. 导出与写库页
- [x] `QT-6-1` 导出排课结果到 txt（默认 `output/plan.txt`）。
- [x] `QT-6-2` 导出后选择是否写入 `student_course`（二次确认）。
- [x] `QT-6-3` 写入模式：追加去重 / 覆盖该学生计划后写入。
- [x] `QT-6-4` 写入后回显统计（新增/重复/删除数量）。

完成标准：
- 导出与数据库写入闭环可在 UI 完成。

## 7. 体验优化
- [ ] `QT-7-1` 增加输入校验提示（必填、数值、范围）。
- [ ] `QT-7-2` 表格支持排序与刷新。
- [ ] `QT-7-3` 长耗时操作加“进行中”状态提示（避免无响应感）。
- [ ] `QT-7-4` 错误信息统一为“用户可读”格式。

完成标准：
- 操作链路清晰、提示明确、无明显卡顿误解。

## 8. 验收与提交
- [ ] `QT-8-1` 编写 Qt 阶段测试记录（功能点 + 截图）。
- [ ] `QT-8-2` 清理临时文件与无用产物。
- [ ] `QT-8-3` 更新主 `TODO.md` 关联 Qt 进度。
- [ ] `QT-8-4` 提交 Git（按模块分 commit）。

完成标准：
- Qt 版本可演示、可构建、可提交。
